# PostgreSQL 数据库配置
DB_HOST=localhost
DB_PORT=5555
DB_NAME=magsync
DB_USER=postgres
DB_PASSWORD=MyyKzVh5bpaacCEBiOB39Ddacc

# 使用说明：
# 1. 复制此文件为 .env
# 2. 修改上述配置以匹配你的 PostgreSQL 设置
# 3. 确保你的数据库中有 config 表，包含以下结构：
#    - key (varchar): 存储配置键
#    - value (txt): 存储配置值, 为 JSON 数据
#    - updated_at (timestamp): 更新时间戳

# Contains AI-generated edits.
